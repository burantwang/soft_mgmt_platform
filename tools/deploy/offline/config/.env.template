# ============================================================
# 部署环境变量（安装前请修改，安装脚本会自动生成随机值）
# 权限要求：chmod 600 /opt/devplatform/config/.env
# ============================================================

# MySQL root 密码（同时用于后端连接）
MYSQL_ROOT_PASSWORD=ChangeMe_Mysql_2026

# Jenkins/外部系统推送报告用的令牌
API_PUSH_TOKEN=ChangeMe_Push_Token_2026

# 超管初始密码（首次登录强制修改）
ADMIN_INIT_PASSWORD=Admin@123
