# ============ 依赖与构建产物（可重新生成，不随代码迁移） ============
node_modules/
backend/target/
frontend/dist/
*.log

# ============ 运行时数据（随环境迁移，不走版本库） ============
backend/data/
data/

# ============ IDE / 系统文件 ============
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ============ 本机运行状态 / 临时解包产物（不入库） ============
.codebuddy/
tools/_jar_extract/

# ============ 环境配置（含密码，不入库） ============
.env
.env.local
